# ⚠️⚠️ 本文件**只管静态资源**。Cloudflare Pages 的 `_headers` 对 Functions 自己
#     `new Response(...)` 出来的响应**完全不生效** —— 2026-09-17 线上实测：
#       /assets/style.css → nosniff ✓        /sitemap.xml → 一个安全头都没有
#     所以 /api/*（登录、注册、留言…）与三个 XML/TXT 端点的安全头**不在这个文件里**，
#     在 functions/_lib/security.js + functions/_middleware.js。
#     两边是**两份拷贝**（这里是纯文本，没法 import），改一边必须改另一边 ——
#     scripts/verify-security-headers.mjs 会逐个头比对，漏改就判红。

# 全局安全头：对所有静态资源生效（含 /404.html、/admin/ 之类）
/*
  X-Content-Type-Options: nosniff
  Referrer-Policy: strict-origin-when-cross-origin
  Strict-Transport-Security: max-age=31536000
  X-Frame-Options: DENY
  Permissions-Policy: geolocation=(), camera=(), microphone=(), payment=(), usb=(), magnetometer=(), gyroscope=(), accelerometer=(), midi=(), serial=(), hid=()
  Content-Security-Policy: object-src 'none'; base-uri 'self'; frame-ancestors 'none'

# 静态资源：长缓存 + 后台再验证（stale-while-revalidate）。
# 跨境网络抖动时浏览器直接命中本地缓存秒开，后台静默校验更新，
# 避免「刷新后 JS 不跑 / 读不到缓存」的问题（pages.dev 在大陆无节点，每次强校验都会跨境重下）。
# 注意：不要用更具体的 /assets/app.js 再写一遍，否则两条规则都生效、响应头会被重复拼接。
/assets/*
  Cache-Control: public, max-age=86400, stale-while-revalidate=604800

/content/*
  Cache-Control: public, max-age=3600, stale-while-revalidate=86400

# 封面图：文件名带内容哈希（build.mjs 生成），换封面即换文件名，一年 immutable。
/generated/covers/*
  Cache-Control: public, max-age=31536000, immutable

# 封面映射表（slug → 封面路径，build.mjs 生成，供边缘函数注入 og:image）。
# 必须短缓存：新部署会换掉封面文件名，长缓存会让边缘拿到旧映射表、指向已删除的文件。
# 注意这条要写在 /generated/covers/* 之后，且 covers.json 不匹配上面那条通配（它需要多一层路径）。
/generated/covers.json
  Cache-Control: public, max-age=60

# 列表/详情快照：长缓存 + 后台再验证（SWR）。
# 前端用 cache:"default" 交给这套头（先命中本地缓存秒开，后台再校验更新）；
# 另由 /api/posts/meta 比对 count/latest 做新鲜度自检并静默补最新文章。
/generated/posts.json
  Cache-Control: public, max-age=300, stale-while-revalidate=86400

/generated/posts/*
  Cache-Control: public, max-age=300, stale-while-revalidate=86400

# 爬虫用正文片段（build.mjs 预渲染）。文件名是 slug 的哈希（纯 ASCII），
# 但**内容会随文章更新而变**，所以不能 immutable —— 与详情快照同策略：短缓存 + 后台再验证。
/generated/post-html/*
  Cache-Control: public, max-age=300, stale-while-revalidate=86400

# 正文片段映射表（slug → 片段路径）：同 covers.json，必须短缓存。
# 新部署会换掉片段文件名，长缓存会让边缘拿到旧映射表、指向已删除的片段。
# 同样注意：这条要写在 /generated/post-html/* 之后，且它不匹配上面那条通配（多一层路径）。
/generated/post-html.json
  Cache-Control: public, max-age=60

# 正文内嵌图：由 build.mjs 从 base64 解码落盘，文件名 = 内容哈希。
# 内容变即换名，所以可以放一年 immutable。
/generated/body-images/*
  Cache-Control: public, max-age=31536000, immutable

# 首页 HTML 外壳：必须「每次都校验」，绝不能让旧壳长期生效。
# 事故（2026-09-12 实测）：这里原本是 max-age=60 + stale-while-revalidate=86400，
# 浏览器/边缘可以端着近一天的旧壳，而旧壳里引用的资源文件名是按内容哈希生成的构建产物，
# 下次部署后就没了 → app.js 404 → 整站 JS 全废（列表空白、骨架屏永远不动）。
# 现在 build.mjs 改为「稳定文件名 + ?v=内容哈希」，旧壳最多加载上一版资源、不会再 404；
# 但 HTML 仍必须及时换新，所以 max-age=0：每次导航都条件请求（有 ETag，命中就是 304，很便宜），
# 只给 300 秒 SWR 兜住跨境抖动时的「先显示旧壳、后台静默更新」。
/
  Cache-Control: public, max-age=0, stale-while-revalidate=300
